[build]
  publish = "."
  functions = "api"
  command = "npm install"

[[redirects]]
  from = "/"
  to = "/family-finance.html"
  status = 200

[dev]
  command = "echo 'No build step needed'"
  port = 8888
  targetPort = 8888
